// Минимальный entrypoint для Vercel
// Этот файл нужен только для того, чтобы Vercel не искал entrypoint
// Все запросы обрабатываются автоматически:
// - /api/* -> serverless функции в папке api/
// - статические файлы -> отдаются напрямую
// - остальное -> index.html

module.exports = (req, res) => {
  // Этот handler никогда не должен вызываться, так как
  // Vercel автоматически обрабатывает статические файлы и API
  res.status(404).json({ error: 'Not found' });
};
